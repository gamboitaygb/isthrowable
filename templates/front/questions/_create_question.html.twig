{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Create Question!{% endblock %}
{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
        <div class="container is-c-home">
            <div class="row">
                <div class="col-12">
                    <h3 class="text-center">{{ '¡Haz tu pregunta!'|trans }}</h3>
                </div>
                <section class="col-12 col-md-8 mx-auto basic">
                    {{ form_start(form) }}
                    <div class="form-group">
                        {{ form_widget(form.title) }}
                    </div>
                    <div class="form-group">
                        {{ form_widget(form.content) }}
                    </div>
                    {{ form_end(form) }}
                </section>
            </div>
        </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script type="application/javascript" src="{{ asset('build/ckeditor/ckeditor.js') }}"></script>
    <script type="text/javascript" src="{{ asset('build/questionjs.js') }}"></script>
{% endblock %}