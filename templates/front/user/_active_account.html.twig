{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Error!{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('build/login.css') }}" rel="stylesheet" />
{% endblock %}

{% block body %}

    {% block header %}{% endblock %}

    <div class="container  is-login">
        <div class="flex-row">
            <div class="col-12 col-md-4 mx-md-auto">
                <a class="navbar-brand" href="{{ url('user_login') }}"><img class="logo" src="{{ asset('build/static/logo.jpg') }}" /> </a>
                <div class="is-box">
                    {% if success == true and key == true %}
                        <p>Gracias por activar su cuenta, para iniciar sesión <a href="{{ url('user_login') }}">Click aquí</a> </p>
                    {% elseif sucess == true and key == false %}
                        <p>Lo sentimos, su cuenta no se ha podido actuvar, puedes volver a registrarte o escribir a info@isthrowable.com</p>
                    {% elseif success == false and key == false %}
                        <p>Lo sentimos pero ya ese link a cadugado, pongase en contacto mediante info@isthrowable.com</p>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}